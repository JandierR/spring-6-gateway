spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: spring-7-rest-mvc
              uri: http://localhost:8081
              predicates:
                - Path=/api/v1/*